class Sammarks {
    public static void main(String[] args) {

        // Marks in each subject
        double mathsMarks = 94;
        double physicsMarks = 95;
        double chemistryMarks = 96;

        // Compute average
        double averagePCM = (mathsMarks + physicsMarks + chemistryMarks) / 3;

        // Output
        System.out.println("Sam’s average mark in PCM is " + averagePCM);
    }
}

